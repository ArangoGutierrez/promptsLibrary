---
description: Core dispatch + essential constraints
alwaysApply: true
---
# Core

## ESSENTIAL
- No secrets/tokens in code
- No `// ... existing ...` placeholders
- Verify before claim: file:line must exist
- ≤3 questions, else proceed+assumptions

## DEPTH (invoke /deep for full)
enumerate≥3|no-first-solution|doubt-verify

## TOKEN
ref>paste:`path:line`|table>prose|no-filler|delta-only

## MODES (invoke to load)
`/deep` deep analysis|`/security` security audit|`/perf` performance

## VERIFY
claim→Q→answer INDEPENDENTLY→✓keep/✗drop
